"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var assert = require('chai').assert;
var async = require('async');
var CommandExec_1 = require("./CommandExec");
var Command_1 = require("../../src/commands/Command");
var Parameters_1 = require("../../src/run/Parameters");
suite('Command', function () {
    test('Get Name', function (done) {
        var command = new Command_1.Command("name", null, new CommandExec_1.CommandExec());
        // Check match by individual fields
        assert.isNotNull(command);
        assert.equal(command.getName(), 'name');
        done();
    });
    test('Execute', function (done) {
        var command = new Command_1.Command("name", null, new CommandExec_1.CommandExec());
        var map = {};
        map[8] = "title 8";
        map[11] = "title 11";
        var param = new Parameters_1.Parameters(map);
        command.execute("a", param, function (err, result) {
            assert.equal(result, 0);
            done();
        });
    });
});
//# sourceMappingURL=Command.test.js.map