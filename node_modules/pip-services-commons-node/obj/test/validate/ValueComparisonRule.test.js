"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var assert = require('chai').assert;
var Schema_1 = require("../../src/validate/Schema");
var ValueComparisonRule_1 = require("../../src/validate/ValueComparisonRule");
suite('ValueComparisonRule', function () {
    test('Number Equal Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("EQ", 123));
        var results = schema.validate(123);
        assert.equal(results.length, 0);
        results = schema.validate(432);
        assert.equal(results.length, 1);
        done();
    });
    test('String Equal Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("EQ", "ABC"));
        var results = schema.validate("ABC");
        assert.equal(results.length, 0);
        results = schema.validate("XYZ");
        assert.equal(results.length, 1);
        done();
    });
    test('Number Not Equal Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("NE", 123));
        var results = schema.validate(123);
        assert.equal(results.length, 1);
        results = schema.validate(432);
        assert.equal(results.length, 0);
        done();
    });
    test('String Not Equal Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("NE", "ABC"));
        var results = schema.validate("ABC");
        assert.equal(results.length, 1);
        results = schema.validate("XYZ");
        assert.equal(results.length, 0);
        done();
    });
    test('Less Than or Equal Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("LE", 123));
        var results = schema.validate(123);
        assert.equal(results.length, 0);
        results = schema.validate(432);
        assert.equal(results.length, 1);
        done();
    });
    test('Less Than Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("LT", 123));
        var results = schema.validate(123);
        assert.equal(results.length, 1);
        results = schema.validate(0);
        assert.equal(results.length, 0);
        done();
    });
    test('More Than or Equal Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("GE", 123));
        var results = schema.validate(123);
        assert.equal(results.length, 0);
        results = schema.validate(432);
        assert.equal(results.length, 0);
        results = schema.validate(0);
        assert.equal(results.length, 1);
        done();
    });
    test('More Than Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("GT", 123));
        var results = schema.validate(123);
        assert.equal(results.length, 1);
        results = schema.validate(432);
        assert.equal(results.length, 0);
        results = schema.validate(0);
        assert.equal(results.length, 1);
        done();
    });
    test('Match Comparison', function (done) {
        var schema = new Schema_1.Schema().withRule(new ValueComparisonRule_1.ValueComparisonRule("LIKE", "A.*"));
        var results = schema.validate("ABC");
        assert.equal(results.length, 0);
        results = schema.validate("XYZ");
        assert.equal(results.length, 1);
        done();
    });
});
//# sourceMappingURL=ValueComparisonRule.test.js.map