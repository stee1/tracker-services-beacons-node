"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var assert = require('chai').assert;
var ConfigParams_1 = require("../../src/config/ConfigParams");
var NameResolver_1 = require("../../src/config/NameResolver");
suite('JsonConfigReader', function () {
    test('Read Config', function (done) {
        var config = ConfigParams_1.ConfigParams.fromTuples("id", "ABC");
        var name = NameResolver_1.NameResolver.resolve(config);
        assert.equal(name, 'ABC');
        var config = ConfigParams_1.ConfigParams.fromTuples("name", "ABC");
        var name = NameResolver_1.NameResolver.resolve(config);
        assert.equal(name, 'ABC');
        done();
    });
    test('Empty Name', function (done) {
        var config = ConfigParams_1.ConfigParams.fromTuples();
        var name = NameResolver_1.NameResolver.resolve(config);
        assert.isNull(name);
        done();
    });
});
//# sourceMappingURL=NameResolver.test.js.map