"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var assert = require('chai').assert;
var IdGenerator_1 = require("../../src/data/IdGenerator");
suite('IdGenerator', function () {
    test('Short Id', function () {
        var id1 = IdGenerator_1.IdGenerator.nextShort();
        assert.isNotNull(id1);
        assert.isTrue(id1.length >= 9);
        var id2 = IdGenerator_1.IdGenerator.nextShort();
        assert.isNotNull(id2);
        assert.isTrue(id2.length >= 9);
        assert.isFalse(id1 == id2);
    });
    test('Long Id', function () {
        var id1 = IdGenerator_1.IdGenerator.nextLong();
        assert.isNotNull(id1);
        assert.isTrue(id1.length >= 32);
        var id2 = IdGenerator_1.IdGenerator.nextLong();
        assert.isNotNull(id2);
        assert.isTrue(id2.length >= 32);
        assert.isFalse(id1 == id2);
    });
});
//# sourceMappingURL=IdGenerator.test.js.map